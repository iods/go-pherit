// Chatter gRPC Demo .proto file
syntax = "proto3";

option go_package = "./alpha;alpha";

message Message {
    string body = 1;
}

service ServiceAlpha {
    rpc GetText(Message) returns (Message) {}
}