package datatypes

import (
	"fmt"
	"strings"
)

// SunsetTypes Exportable function for representing the various boolean
//   operations available in Go.
func SunsetTypes() {
	title := "Sunset: Boolean Types in Go."
	fmt.Println(strings.Title(title))
}